const FeatureProject = ({ featureProject }) => {
  const { projectType, projectName, address, projectImg, projectVideo } =
    featureProject;

  return (
    <div>
      {/* <h1>{projectType}</h1> */}
    </div>
  );
};

export default FeatureProject;
